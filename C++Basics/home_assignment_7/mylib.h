#pragma once

namespace task_5
{
	void InitializeArray(float* arr, const size_t size);

	void PrintArray(float* arr, const size_t size);

	void CountElements(float* arr, const size_t size, int* positive, int* negative);
}